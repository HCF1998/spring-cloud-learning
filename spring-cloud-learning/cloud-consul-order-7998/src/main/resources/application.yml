server:
  port: 7998

spring:
  application:
    name: consul-consumer-order
  # consul
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
      # hostname 127.0.0.1